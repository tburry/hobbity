---
/**
 * Inline death dagger icon. Renders the same markup as the rehype-death-dagger
 * plugin, but usable anywhere in mdx (not just headings).
 *
 * Usage: <Dead />
 */
---
<dfn title="Dead" class="icon-dfn">†</dfn>
