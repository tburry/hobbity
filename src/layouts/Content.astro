---
import Base from './Base.astro';

interface Props {
  title?: string;
}

const { title } = Astro.props;
---

<Base title={title} showHome={true} searchable={true}>
  <article>
    <slot />
  </article>
  <slot name="footer" slot="footer" />
</Base>
